
{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "User profile with wallet balance and role",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "walletBalance": { "type": "number", "description": "Current balance in Naira" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "email", "walletBalance", "role"]
    },
    "Transaction": {
      "title": "Transaction",
      "description": "Records of all wallet debits and credits",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "type": { "type": "string", "enum": ["airtime", "data", "funding", "social_log", "number"] },
        "service": { "type": "string" },
        "network": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "amount": { "type": "number" },
        "status": { "type": "string", "enum": ["Pending", "Completed", "Failed"] },
        "date": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "type", "amount", "status", "date"]
    },
    "FundRequest": {
      "title": "Funding Request",
      "description": "Manual wallet funding requests for admin approval",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "amount": { "type": "number" },
        "proofImage": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "approved", "rejected"] },
        "date": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "amount", "status", "date"]
    },
    "Product": {
      "title": "Product Plan",
      "description": "A purchasable item or plan (Data, Number, or Social Log)",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "price": { "type": "number" },
        "type": { "type": "string" },
        "description": { "type": "string" },
        "network": { "type": "string" },
        "imageUrl": { "type": "string" },
        "features": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["name", "price"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/backend/entities/User" },
      "description": "User profiles and roles"
    },
    "/transactions/{transactionId}": {
      "schema": { "$ref": "#/backend/entities/Transaction" },
      "description": "Financial transaction logs"
    },
    "/fundRequests/{requestId}": {
      "schema": { "$ref": "#/backend/entities/FundRequest" },
      "description": "Wallet funding approval queue"
    },
    "/dataPlans/{planId}": { "schema": { "$ref": "#/backend/entities/Product" }, "description": "Data bundles" },
    "/virtualNumbers/{numberId}": { "schema": { "$ref": "#/backend/entities/Product" }, "description": "Virtual numbers" },
    "/socialLogs/{logId}": { "schema": { "$ref": "#/backend/entities/Product" }, "description": "Aged social accounts" }
  }
}
